#include<stdio.h>

int main()
        {
            char ch=0;
            for(int i=0 ;i<=128;i++)
            {
                printf("ASCII Character %c and Value = %d \n",ch,i);
                ch++;
             }
                return 0;
        }
